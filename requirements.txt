# AI Mosaic Builder
# Python 3.10+ recommended
#
# The Windows installer filters llama-cpp-python from this file and installs
# the CPU/CUDA build separately so the correct hardware runtime is selected.

# ── Desktop UI ────────────────────────────────────────────────────────────────
PySide6>=6.5.0

# ── Local LLM / multimodal vision ────────────────────────────────────────────
# Gemma-4 requires a llama-cpp-python release that provides Gemma4ChatHandler.
llama-cpp-python>=0.3.33,<0.4

# ── Image processing ─────────────────────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# ── Real person detection ────────────────────────────────────────────────────
# YOLO is the primary person-detection backend.
# Do not pin torch here: Ultralytics resolves the compatible PyTorch package.
ultralytics>=8.3.0

# ── Near-duplicate / diversity analysis ─────────────────────────────────────
imagehash>=4.3.0
