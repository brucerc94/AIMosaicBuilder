# AI Mosaic Builder
# Python 3.10+ recommended
#
# This file contains the platform-independent Python dependencies.
# Install llama-cpp-python separately with the Windows installer so the
# correct CPU/CUDA build can be selected for the machine.

# ── Desktop UI ────────────────────────────────────────────────────────────────
PySide6>=6.5.0

# ── Image processing ─────────────────────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# ── Real person detection ────────────────────────────────────────────────────
# YOLO is the primary person-detection backend.
# Do not pin torch here: Ultralytics resolves the compatible PyTorch package.
ultralytics>=8.3.0

# ── Near-duplicate / diversity analysis ─────────────────────────────────────
imagehash>=4.3.0
